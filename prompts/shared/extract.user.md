Primary artifact metadata:
{artifact_json}

Primary artifact card (quotes here are the only valid source for `evidence_excerpt`):
{artifact_card}

Supporting artifacts (context only):
{supporting_artifacts_text}

Extract patterns per the system instructions. Return JSON only.
